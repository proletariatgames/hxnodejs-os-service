# Haxe/hxnodejs externs for the [os-service](https://www.npmjs.com/package/os-service) npm library

Tested using `os-service` version **1.3.0**

Example:
```haxe
import js.npm.OsService;

OsService.add('my-service');

OsService.remove('my-service');
```
